/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const chatsAPI_auto = {

    /**
    * Gets all Chats
    */
    getAll: async function(formData) {
        let response = await axios.get(`${BASE_URL}/chats`, requestOptions);
        return response.data;
    },

    /**
    * Gets an entry from 'Chats' by its primary key
    */
    getById: async function(chatId) {
        let response = await axios.get(`${BASE_URL}/chats/${chatId}`, requestOptions);
        return response.data[0];
    },

    /**
    * Creates a new entry in 'Chats'
    */
    create: async function(formData) {
        let response = await axios.post(`${BASE_URL}/chats`, formData, requestOptions);
        return response.data;
    },

    /**
    * Updates an existing entry in 'Chats' by its primary key
    */
    update: async function(formData, chatId) {
        let response = await axios.put(`${BASE_URL}/chats/${chatId}`, formData, requestOptions);
        return response.data;
    },

    /**
    * Deletes an existing entry in 'Chats' by its primary key
    */
    delete: async function(chatId) {
        let response = await axios.delete(`${BASE_URL}/chats/${chatId}`, requestOptions);
        return response.data;
    },
};

export {chatsAPI_auto};