/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const messagesAPI_auto = {

    /**
    * Gets all messages
    */
    getAll: async function(formData) {
        let response = await axios.get(`${BASE_URL}/messages`, requestOptions);
        return response.data;
    },

    /**
    * Gets an entry from 'messages' by its primary key
    */
    getById: async function(messageId) {
        let response = await axios.get(`${BASE_URL}/messages/${messageId}`, requestOptions);
        return response.data[0];
    },

    /**
    * Creates a new entry in 'messages'
    */
    create: async function(formData) {
        let response = await axios.post(`${BASE_URL}/messages`, formData, requestOptions);
        return response.data;
    },

    /**
    * Updates an existing entry in 'messages' by its primary key
    */
    update: async function(formData, messageId) {
        let response = await axios.put(`${BASE_URL}/messages/${messageId}`, formData, requestOptions);
        return response.data;
    },

    /**
    * Deletes an existing entry in 'messages' by its primary key
    */
    delete: async function(messageId) {
        let response = await axios.delete(`${BASE_URL}/messages/${messageId}`, requestOptions);
        return response.data;
    },
};

export {messagesAPI_auto};